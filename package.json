{
  "name": "sbs-simlab",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bash scripts/build-all.sh",
    "start": "tsx server/index.ts",
    "dev": "tsx watch server/index.ts",
    "build:landing": "cd landing && npm install && npm run build",
    "build:beer-game": "cd games/beer-game && npm install && npm run build",
    "build:people-express": "cd games/people-express && npm install && npm run build",
    "build:newsvendor": "cd games/newsvendor && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.21.0",
    "socket.io": "^4.8.3",
    "nanoid": "^3.3.11",
    "tsx": "^4.7.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
